{
  label: Zigzag,
  name: zigzag,
  widget: object,
  collapsed: true,
  summary: "Background color: {{fields.backgroundColor}}",
  required: false,
  fields: [
    { label: Disable, name: disable, widget: boolean, required: false },
    { label: Background color, name: backgroundColor, widget: color, required: false },
    {
      label: Content,
      name: zigzagContent,
      widget: object,
      collapsed: true,
      required: false,
      fields: [
        { label: Image, name: image, widget: string, required: false },
        { label: Title, name: title, widget: string, required: false },
        { label: Description, name: description, widget: text, required: false },
        { label: Reverse, name: reverse, widget: boolean, required: false },
      ]
    }
  ]
}
